def round_up(x):
    return int(round(x + 0.5))

def round_down(x):
    return int(round(x - 0.5))

sayi = float(input("Bir sayı gir: "))

print("Yukarı yuvarlama:", round_up(sayi))
print("Aşağı yuvarlama:", round_down(sayi))